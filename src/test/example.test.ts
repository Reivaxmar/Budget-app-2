import { describe, it, expect } from 'vitest'

describe('Example test suite', () => {
  it('should pass', () => {
    expect(true).toBe(true)
  })

  it('should do a simple math', () => {
    expect(1 + 1).toBe(2)
  })
})
